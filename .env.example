# Warelay Configuration
# Copy this file to .env and configure as needed

# No environment variables required for Baileys-based WhatsApp connection
# All configuration is done via ~/.warelay/warelay.json
# WhatsApp credentials are stored in ~/.warelay/credentials/

# Example: Enable debug logging
# DEBUG=baileys:*
